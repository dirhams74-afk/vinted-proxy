# Port d'écoute (Railway/Render le définissent automatiquement)
PORT=3000

# Clé secrète pour protéger ton proxy (optionnel mais recommandé)
# Si défini, chaque requête doit avoir le header : X-Secret: ta-cle-secrete
PROXY_SECRET=change-moi-avec-un-mot-de-passe-fort
